#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

# Auto-fix (never fail this step)
pnpm prune-imports || true
git add -A

# Verify: fail commit if errors remain
pnpm exec biome check . || {
  echo "Biome still reports errors. Fix them before committing."
  exit 1
}
