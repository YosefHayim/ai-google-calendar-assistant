# ============================================================================
# AI Google Calendar Assistant - Frontend Environment Variables
# ============================================================================
# Copy this file to .env.local and fill in your values
# ============================================================================

# ============================================================================
# REQUIRED VARIABLES
# ============================================================================

# ----- Backend API -----
# URL of the backend service (see /be directory)
NEXT_PUBLIC_BACKEND_URL=http://localhost:3000

# ----- Supabase -----
# Get these from your Supabase project settings > API
# Note: Use the ANON key here, not the service role key
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# ============================================================================
# NOTES
# ============================================================================
#
# 1. All NEXT_PUBLIC_ prefixed variables are exposed to the browser.
#    Never put sensitive keys here - they go in the backend.
#
# 2. The frontend relies on the backend for:
#    - Authentication (Google OAuth flow)
#    - Calendar API calls
#    - AI/Chat functionality
#    - Voice features (LiveKit token generation)
#    - Payment processing
#
# 3. Make sure the backend is running before starting the frontend:
#    cd be && bun dev
#
# 4. The frontend runs on port 4000 by default:
#    cd fe && npm run dev
#
# ============================================================================
